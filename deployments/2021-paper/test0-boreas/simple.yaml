apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple
spec:
  selector:
    matchLabels:
      app: simple
  replicas: 190
  template:
    metadata:
      labels:
        app: simple
    spec:
      schedulerName: boreas-scheduler
      containers:
      - name: simple-container
        image: k8s.gcr.io/pause
        resources:
          requests:
            cpu: "190m"
            memory: "380M"
